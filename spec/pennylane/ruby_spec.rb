# frozen_string_literal: true

RSpec.describe Pennylane::Ruby do
  it "has a version number" do
    expect(Pennylane::Ruby::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
